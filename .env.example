# =============================================================================
# ENVIRONMENT VARIABLES TEMPLATE
# =============================================================================
# Copy this file to create your local configuration:
#    cp .env.example .env.local
#
# NEVER commit .env.local to Git (already in .gitignore)
# =============================================================================

# -----------------------------------------------------------------------------
# Server Configuration
# -----------------------------------------------------------------------------
PORT=3001
NODE_ENV=development

# -----------------------------------------------------------------------------
# Database - Supabase PostgreSQL
# -----------------------------------------------------------------------------
# Get credentials: https://app.supabase.com/project/_/settings/database
#
# DATABASE_URL: Pooler connection (port 6543) → Use for application queries
# DIRECT_URL: Direct connection (port 5432) → Use for Prisma migrations
#
# Replace placeholders:
#   [PROJECT-REF] → Your Supabase project ID (e.g., beaagboxjwqqvlcdtpjj)
#   [YOUR-PASSWORD] → Your database password
#   [REGION] → Your AWS region (e.g., eu-west-1, us-east-1)
# -----------------------------------------------------------------------------
DATABASE_URL="postgresql://postgres.[PROJECT-REF]:[YOUR-PASSWORD]@aws-0-[REGION].pooler.supabase.com:6543/postgres?pgbouncer=true"
DIRECT_URL="postgresql://postgres.[PROJECT-REF]:[YOUR-PASSWORD]@aws-0-[REGION].pooler.supabase.com:5432/postgres"

# -----------------------------------------------------------------------------
# Supabase - Public Configuration
# -----------------------------------------------------------------------------
# Get from: https://app.supabase.com/project/_/settings/api
#
# NEXT_PUBLIC_* variables are exposed to browser (safe for client-side)
# -----------------------------------------------------------------------------
NEXT_PUBLIC_SUPABASE_URL="https://[PROJECT-REF].supabase.co"
NEXT_PUBLIC_SUPABASE_ANON_KEY="your-anon-public-key-here"

# -----------------------------------------------------------------------------
# Supabase - Service Role Key (CRITICAL SECRET)
# -----------------------------------------------------------------------------
# Get from: https://app.supabase.com/project/_/settings/api
#
# KEEP SECRET - Never expose to frontend
# This key bypasses Row Level Security (full admin access)
# -----------------------------------------------------------------------------
SUPABASE_SERVICE_ROLE_KEY="your-service-role-jwt-token"

# -----------------------------------------------------------------------------
# JWT Authentication Secrets
# -----------------------------------------------------------------------------
# Generate secure random secrets:
#    openssl rand -hex 64
#
# Use DIFFERENT secrets for each environment (dev/staging/prod)
# NEVER share or commit these secrets
# JWT_SECRET and JWT_REFRESH_SECRET must be different
# -----------------------------------------------------------------------------
JWT_SECRET="your-256-bit-random-secret-minimum-32-chars"
JWT_REFRESH_SECRET="your-different-256-bit-refresh-secret-minimum-32-chars"